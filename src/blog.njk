---
layout: layouts/page.njk
title: Blog
description: All my blog posts
permalink: /blog/
---

<div class="blog-page">
  <h1>📝 Blog Posts</h1>
  
  <p class="blog-intro">
    Welcome to my blog! Here's where I share my thoughts, projects, and random musings. 
    Enjoy your stay and feel free to leave comments!
  </p>
  
  <div class="divider">
    <img src="/images/dividers/rainbow-line.gif" alt="" aria-hidden="true" onerror="this.style.display='none'">
  </div>
  
  {% if collections.posts.length %}
    <div class="posts-list">
      {% for post in collections.posts %}
      <article class="post-preview">
        <h2>
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h2>
        
        <div class="post-meta">
          <time datetime="{{ post.date | htmlDateString }}">
            📅 {{ post.date | readableDate }}
          </time>
          
          {% if post.data.tags %}
          <div class="post-tags">
            {% for tag in post.data.tags %}
              {% if tag != "posts" %}
              <a href="/tags/{{ tag | slug }}/" class="tag">{{ tag }}</a>
              {% endif %}
            {% endfor %}
          </div>
          {% endif %}
        </div>
        
        {% if post.data.description %}
        <p class="post-description">{{ post.data.description }}</p>
        {% endif %}
        
        <a href="{{ post.url }}" class="read-more-btn">Read Post →</a>
      </article>
      
      <div class="divider">
        <img src="/images/dividers/stars-line.gif" alt="" aria-hidden="true" onerror="this.style.display='none'">
      </div>
      {% endfor %}
    </div>
  {% else %}
    <div class="no-posts">
      <p>No posts yet! Check back soon for updates. 🚧</p>
      <img src="/images/under-construction.gif" alt="Under construction" style="max-width: 200px; margin: 1rem auto;" onerror="this.style.display='none'">
    </div>
  {% endif %}
  
</div>

<style>
.blog-page {
  font-family: var(--font-body);
}

.blog-intro {
  font-size: 1.25rem;
  text-align: center;
  margin-bottom: 2rem;
}

.posts-list {
  margin-top: 2rem;
}

.post-preview {
  margin-bottom: 2rem;
}

.post-preview h2 {
  margin-bottom: 1rem;
}

.post-preview h2 a {
  color: inherit;
  text-decoration: none;
}

.post-preview h2 a:hover {
  color: #ff00ff;
  text-decoration: underline;
}

.post-meta {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
  align-items: center;
  margin-bottom: 1rem;
  font-size: 1rem;
}

.post-tags {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
}

.post-description {
  font-size: 1.125rem;
  margin-bottom: 1rem;
  line-height: 1.6;
}

.read-more-btn {
  display: inline-block;
  padding: 0.5rem 1.5rem;
  background: linear-gradient(135deg, #ff00ff, #00ffff);
  color: #000;
  border: 3px solid #000;
  text-decoration: none;
  font-weight: bold;
  font-family: var(--font-display);
  font-size: 0.875rem;
  transition: all 0.2s;
}

.read-more-btn:hover {
  background: linear-gradient(135deg, #00ffff, #ffff00);
  transform: translateY(-2px);
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}

.no-posts {
  text-align: center;
  padding: 3rem;
  background: rgba(255, 255, 0, 0.2);
  border: 3px dashed #000;
}
</style>
